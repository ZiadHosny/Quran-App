import express from "express";
const userPlaylistRouter = express.Router();
// userPlaylistRouter.post('/', auth, AddSongToUserPlaylist)
// userPlaylistRouter.get('/', auth, getUserPlaylist)
// userPlaylistRouter.delete('/', auth, removeSongFromUserPlaylist)
export default userPlaylistRouter;
